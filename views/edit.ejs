<!DOCTYPE html>
<html>
<head>
  <title>Edit User</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center h-screen">
  <form action="/edit/<%= user._id %>" method="POST" class="bg-zinc-800 p-6 rounded-lg shadow-lg w-96">
    <h2 class="text-4xl font-bold mb-4">Edit User</h2>

    <label class="block mb-2 text-xl  ">Name:</label>
    <input type="text" name="name" value="<%= user.name %>" class="w-full p-2 mb-4 rounded text-black border border-4" required>

    <label class="block mb-2 text-xl">Email:</label>
    <input type="email" name="email" value="<%= user.email %>" class="w-full p-2 mb-4 rounded text-black border" required>

    <label class="block mb-2 text-xl">Image URL:</label>
    <input type="text" name="imageurl" value="<%= user.imageurl %>" class="w-full p-2 mb-4 rounded text-black border ">

    <button type="submit" class="bg-blue-500 px-4 py-2 rounded hover:bg-blue-600">Update</button>
    <a href="/read" class="ml-3 text-gray-300 hover:underline">Cancel</a>
  </form>
</body>
</html>
