<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Backend Work</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 h-screen text-white">  
    <br>    
    <a href="/" class="text-blue-500 hover:underline ml-5"> BACK TO HOMEPAGE    </a>
      <h1 class="text-6xl mt-5 ml-5 font-bold">ALL USER</h1>
      <br>
 
<!-- Read Work  -->
    <div class="flex flex-wrap gap-5 ml-[90px]">
  <% if (user.length > 0) { %>
    <% user.forEach((user) => { %>
      <div class="bg-zinc-800 w-50  sm:w-[300px] text-white p-4 rounded-lg">
        <div class="w-full h-75 rounded-lg overflow-hidden">
          <img src="<%= user.imageurl %>" alt="User Image" class="w-full h-75 object-cover" />
        </div>
        <h3 class="mt-3 text-xl"><%= user.name %></h3>
        <h5 class="text-zinc-500"><%= user.email %></h5>
        <div class="flex justify-between mt-3">
          
          <!-- Edit Work -->
          <a href="/edit/<%= user._id %>" class="text-blue-500 hover:underline">EDIT</a>
          
          <!-- Delete Work -->
          <form action="/delete/<%= user._id %>" method="POST" style="display:inline;">
            <button type="submit" class="text-red-500 hover:underline" onclick="return confirm('Are you sure you want to delete this user?');">
              DELETE
            </button>
          </form>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <div class="text-center w-full mt-10">
      <h2 class="text-2xl text-gray-400">🚫 No users yet!</h2>
      <p class="text-gray-500">Add a new user to see them here.</p>
    </div>
  <% } %>
</div>




 </div>

       


      </div>

  </body>
</html>
